package tryexceptelse.viseq.model;

/**
 * Handles conversion from image to String equation.
 */
public class ImageParser {

    public String parse(final byte[] data) {
        return "f(x) = 2x + 1";  // PLACEHOLDER
    }
}
